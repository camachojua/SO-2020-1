	.global _start
	.text
_start:
	movq $30, %rax
	movq $0, %rdx

	movq $9, %rbx

while:
	div %rbx
	cmp $0, %rdx
	mov %rdx, %r8
	mov %rbx, %rax
	mov $0, %rdx
	mov %r8, %rbx
	jne while

	mov $60, %rax
	mov %rbx, %rdi
	syscall
	
